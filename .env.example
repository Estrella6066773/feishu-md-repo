# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略，不会进入 Git）
# 本项目的飞书 App Secret 等敏感信息应通过管理面板写入本地 SQLite，不要写进 .env。

# core-service 监听地址（默认仅本机，勿对公网暴露）
FEISHU_MD_HOST=127.0.0.1
FEISHU_MD_PORT=8787

# 可选：覆盖本地数据目录（内含 app.db，含凭证与同步状态）
# Windows 默认：%AppData%/Roaming/feishu-md-repo
# FEISHU_MD_DATA_DIR=
